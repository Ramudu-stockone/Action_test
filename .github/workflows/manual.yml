# This is a basic workflow that is manually triggered

name: Ram workflow random

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
   workflow_dispatch

jobs:
  create-pull-request:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
    
      - name: run hub commands
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
         MYVAR=$(curl \
         -H "Accept: application/vnd.github+json" \
         -H "Authorization: Bearer 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" \
         https://api.github.com/repos/Ramudu-stockone/Action_test/branches)
         echo $MYVAR
